# Inception
Building a containerized infrastructure using Docker and Docker Compose with custom images (no prebuilt images). Includes Dockerfiles, isolated bridge networks, persistent volumes for MariaDB and WordPress, and an NGINX reverse proxy with TLS, following DevOps best practices.
